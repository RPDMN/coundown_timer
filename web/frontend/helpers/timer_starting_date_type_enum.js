// Frozen enum - Immutable
// THis will tell whether the timer will start now or later in time
const TIMER_STARTING_DATE_TYPE = Object.freeze({
  NOW: "NOW",
  LATER: "LATER",
});

export default TIMER_STARTING_DATE_TYPE;
